package gvprojects.chess.model;

public class Queen extends ChessPiece {

	public Queen(Player p) {
		super(p, "Queen");
	}

	@Override
	public boolean isValidMove(Move m, IChessPiece[][] p) {
		if (super.isValidMove(m, p) == false)
			return false;
		else if (!(m.toRow != m.fromRow && m.toColumn != m.fromColumn)
				|| checkRandC(m, p) == false)
			return false;
		else if (m.toRow != m.fromRow && m.toColumn != m.fromColumn || checkDiag(m, p) == false)
			return false;
		return true;
	}

}
